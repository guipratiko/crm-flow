# Easypanel/Nixpacks: instalar devDependencies no build (prisma generate, etc.)
[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node scripts/prisma-migrate.mjs && node dist/server.js"
